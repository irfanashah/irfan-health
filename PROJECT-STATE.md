# Project State — Irfan's Health Platform

_Last updated: 2026-07-30 (session: L16a resolved — meal-day boundary = 04:00 GST per Irfan; backlog now 0 CRITICAL / 0 HIGH / 14 MEDIUM / 0 LOW, nothing flagged)_
_Authoritative live build record is `CLAUDE.md`. This file is the concise snapshot; Cowork mirrors it into memory. If the two disagree, CLAUDE.md wins._

## Now
**L16a resolved — meal-day boundary is 04:00 GST, not midnight (Irfan's product decision 2026-07-30).** The last flagged backlog item. In `migration_003`'s `meals_enriched`/`meals_daily`, a meal's day is now `meal_day = ((eaten_at AT TIME ZONE 'Asia/Dubai') − INTERVAL '4 hours')::date` — the eating day runs 04:00→04:00, so a meal at 00:00–03:59 GST attributes to the PREVIOUS day (late-night eating counts toward the day you were up); breakfast onward is unchanged. The sleep-onset join key moved in lockstep (`s.wake_day = meal_day + 1`), so a 1am pre-sleep meal both lands on the previous day AND still counts against the sleep it precedes; `is_evening`/`evening_carbs_g`/`last_meal_to_sleep_min` stay consistent. Day-boundary move only (not a wake-day pre-shift) → the correlation engine's +1d lag isn't double-shifted. SQL-view-only, no new table, columns/SELECT order unchanged; `MEAL_DAY_CUTOFF = 04:00` is a commented tunable; hand-verified via worked timestamps in the CTE comment (no SQL test harness). **Deliberately out of scope (flagged):** the food-logging UI's "today's meals" list still groups by midnight GST (`gstDateOf`/`gstDateKey`) — unifying it with the 04:00 boundary is a separate UX call. `npm run lint`/`tsc`/`test` (175)/`build` all clean. Gotcha #166. **Irfan re-runs `migration_003` in Supabase after deploy.**

_Prior session: **Deep-audit LOW tier cleared (L1–L17)** — 16 fixed 2026-07-07 (gotchas #162–165) + L16a (this session). Migrations 003/005/010 were re-run + confirmed applied._ Highlights of the 16: audit-trail honesty (L3 `records_written` ID-diff so re-upserts aren't counted as writes; L4 manual-log pending→finalize; L5 diagnose expected mirrors the adapter's null-skips + boundary predicate), CGM edge (L6 descending+reverse keeps the newest window; L7 timestamp-based `computeCgmTrend` slope), timezone (L9 `timeZone:'Asia/Dubai'` on every user-facing formatter; L10 Timeline absolute-time-until-mount; L11 sign-correct LDL arrow + honest "showing 20 of N"), labs (L8 commit-success banner now shows), anchor/drift (L1 anchor stats 8→13 metrics; L14 med-change metric-id validation), view (L15 date-spine includes Oxylink + meals; L16b sleep-session dedup), robustness (L12 transient-vs-terminal file-drop; L13 `getTokens` no-row-vs-error), odds (L2 seed guard no longer resurrects deactivated meds; L17 removed a dead unguarded `'use server'` export). **Three migrations re-run in Supabase — ✅ confirmed applied by Irfan 2026-07-07** (all idempotent / CREATE OR REPLACE, columns unchanged at END): `migration_003` (L15 + L16b), `migration_005` (L1), `migration_010` (L2). Everything else TS-only. Gotchas #162–165. `npm run lint` 0 errors, `tsc --noEmit` clean, `npm run test` **175 passed** (158→175, +17), `npm run build` clean (33 routes). No live DB here — Irfan sanity-checks the three migrations on re-run.

_Prior session: **Mobile device-width pass — CSS/UI only, every route.**_ Fixed padding + edge-bleed + horizontal-scroll issues so nothing crosses the viewport edge and there's zero horizontal scroll at 320px on every route, both themes; desktop (≥1120px) renders identically and the `/report` print layout is untouched. Systemic: `html, body { overflow-x: clip }` safety net (`clip` not `hidden` — hidden would break the sticky topbar) + the real root-cause fixes — `min-width: 0` on grid/flex children, fluid `minmax(0,1fr)` tracks, `overflow-x:auto` table wrappers. Header/tabnav/range-seg now wrap on mobile (were overflowing the topbar under ~380px). `/report`: new `@media screen` mobile query (padding + tile grids → 1 col) + all 3 tables wrapped in `.report-table-wrap` scroll boxes (with a print-safe `overflow:visible` reset). `/food`: item macros → 2-col grid instead of a 10-input tall stack, and fixed a real overflow bug the harness caught (bare `<input>` intrinsic min-width bursting the totals grid → `min-width:0` + `width:100%`). `/medications`: form + edit rows → 1 col at ≤460. Container padding reduced on labs/meds/food at ≤480. Dashboard Labs-tab table wrapped in a scroll box. `/diagnostics` + `/log` `px-6` → `px-4 sm:px-6`. **Verification:** auth-gated routes can't be logged into here, so verified the actual edited stylesheets against faithful mock markup in a throwaway static harness (getBoundingClientRect sweep at 320/375/414px, both themes → 0 elements crossing the edge; scrollable table wrappers confirmed bounded within the viewport); `/login` checked live; desktop parity confirmed by computed-style sampling at 1280px. `npm run lint` 0 errors, `tsc --noEmit` clean, `npm run test` 158 passed, `npm run build` clean (33 routes). Gotcha #160. **Files (UI/CSS only):** `globals.css`, `dashboard.css`, `report.css` + `Report.tsx` (table wrappers), `food.css`, `medications.css`, `labs.css`, `LabsTab.tsx` (table wrapper), `diagnostics/page.tsx`, `log/page.tsx`.

_Prior session (2026-07-06):_
**Deep-audit backlog — ingestion-reliability cluster fixed (H1/H2/H3/H6). Backlog is now 0 CRITICAL / 0 HIGH.** H1+H3 are coupled (both govern whether a cardiac patient's data is complete) — proposed the frontier design, paused for Irfan's confirmation, built only after approval + one required fix + one doc note he flagged. H2+H6 were pre-approved clean fixes. **H2:** Whoop cron dropped recoveries whose linked cycle started just before the fetch window (cycle ends ~midnight, recovery scored next morning) — the refill route already carried a 14-day backward buffer on the cycle fetch for this exact reason, the cron adapter never got it. Mirrored the refill route's `CYCLE_BUFFER_MS` + guard verbatim. **H6:** a CGM reading of any age within 24h rendered as "CGM · live" with a trend arrow; a 20-hour-stale reading read as current and the fingerstick fallback never engaged. Added `computeCgmFreshness()` (30-min staleness gate) and `computeCgmWear()` (12h TIR-coverage gate) as pure, unit-tested functions in `daily-metrics.ts`; both `GlucosePanel` and `TodayAtAGlance` now gate on `cgmIsLive = latest.cgm !== null && !latest.cgm.stale` instead of just non-null, routing stale CGM into the existing fingerstick-fallback UI rather than a new path. **H1 (frontier lookback):** the old `windowStart = lastEnd − 24h` chased `now` tighter than realistic device sync delay (a Withings cuff can sit unsynced all weekend) — a zero-record "success" run could advance the frontier past a not-yet-synced reading, losing it permanently. **H3 (frontier freeze):** a single permanently-failing row flipped a run to `'partial'` forever, and the old `getLastSuccessfulWindowEnd` matched `'success'` only, so the frontier pinned while the window grew unbounded toward `maxDuration`. Fixed together: new pure `computeIngestWindow()` helper (`adapters/_lib/ingestion-window.ts`) computes `windowStart = max(lastCoveredEnd − lookback, now − maxLookback)`; `getLastCoveredWindowEnd` (renamed) now matches `status IN ('success','partial')`; per-source `INGEST_WINDOW_CONFIG` (Whoop 4d/30d cap, Withings 7d/30d cap, Nightscout 4d/14d cap — all provisional). Nightscout's cron route stopped bypassing the adapter's own frontier logic with a fixed 48h window. **Irfan's required addition during confirmation:** `pipeline-health.ts`'s staleness signal still read `status='success'` only — left unfixed, a recurring bad row would trigger a false "source stale" alert every day for up to `lookback` days even though ingestion was fine. Aligned it to the same `'success'|'partial'` definition. **Irfan's doc note:** the `maxLookback` cap means an outage longer than it leaves a real gap the cron won't self-heal — documented that the refill route (explicit `fromDate`) is the intended recovery path, not the cron (gotcha #159). Withings' native `lastupdate` sync param was considered and explicitly deferred as documented future work (Irfan's call — uniform lookback-widening judged adequate for H1 without the extra API-contract complexity). Gotchas #156–159. `npm run lint` 0 errors, `tsc --noEmit` clean, `npm run build` clean (33 routes unchanged), `npm run test` **158 passed** (146→158, +12 new: 8 for the CGM staleness/wear functions, 4 for the frontier window helper).

_Prior session: **Deep-audit backlog — top 4 items fixed, small high-care pass.** Fixed C1, H7, H4, H5 from the 2026-07-01 audit backlog. **C1 (CRITICAL, security):** the four OAuth connect/callback routes (`app/api/auth/{whoop,withings}/route.ts` + both `callback/route.ts`) had no session guard — `middleware.ts` deliberately exempts `/api/*`, so anyone who knew the app URL could complete OAuth with their own device account and overwrite Irfan's stored tokens. Added the same `getUser()` guard `/api/ingest/*` already uses; CSRF `state` check kept as defence-in-depth. **H7 (security):** open redirect on `?next=` in the Supabase auth callback — extracted a pure `safeRedirectPath()` (`lib/safe-redirect.ts` + `.test.ts`, 6 cases incl. `//evil.com` and backslash-disguised variants). **H4 (data regression):** `last_meal_to_sleep_min` in `migration_003` was fabricating `0` instead of `NULL` on every current day (Postgres `GREATEST(0, NULL) = 0`) — the correlation engine was ingesting a fake covariate reading daily. Fixed with an explicit `CASE WHEN ... IS NULL THEN NULL` guard. Migration re-run confirmed by Irfan 2026-07-01. **H5 (clinical safety):** a real clinical-low breach (e.g. systolic 84) was rendering as a calm "settling"/"steady" card whenever the metric was still `establishing` or alerts were suppressed — `present.ts`'s state remap checked those before checking for a breach. Breach now checked first, unconditionally. Added gotchas #153–155; gotcha #59 marked superseded. `npm run test` 146 passed (133→146, +13 new)._

_Prior session before that (still otherwise the last shipped work before this pass): **Lint cleanup follow-on landed — CI is fully green, zero shortcuts.**_ Closes out the code-review Phase-2 remediation: every one of the ~50 lint errors left advisory last session is now genuinely fixed (no `eslint-disable` anywhere), and the LDL near-goal boundary bug that same session surfaced (and deliberately left unfixed, tracked via `it.fails`) is fixed too. Highlights: `prototype-src/**` + `outputs/**` added to eslint ignores (dead reference code, ~14 errors); real rules-of-hooks fixes (hoisted a conditionally-called `useCallback`/`useRouter`/`useTransition` above early returns in `BaselinesClient.tsx` + 4 chart components); real hydration-risk fixes (`Math.random()` gradient ids → `useId()`; a new `lib/hooks/useIsClient.ts` `useSyncExternalStore`-based hook replaces the `useState`+`useEffect` "mounted" pattern in `ThemeToggle`/`Header`, satisfying the newer `set-state-in-effect` rule the old React-documented idiom was tripping); `Donut.tsx`'s post-render `offset += len` mutation replaced with a precomputed cumulative-offset array. `evaluateLabMarker`'s `goalStateLower`/`Higher`/`Range` now carry a `BOUNDARY_EPS=1e-9` so `1.4+0.2!==1.6` in IEEE754 can't misclassify a real lab value at the documented boundary — `targets.test.ts`'s `it.fails` is now a normal passing test. Visually verified every touched chart via a temporary isolated preview route (deleted before commit) since the real dashboard needs Irfan's login. `.github/workflows/ci.yml`'s Lint step is a hard gate again — no `continue-on-error`. `npm run lint` — **0 errors**. `tsc --noEmit` clean. `npm run test` — **133 passed, 0 expected-fail**. `npm run build` clean — 33 routes unchanged. Background task `task_3927e02c` (lint cleanup) dismissed as done.

## Slice ledger
- ✅ Slices 0–5 (sources + ingestion + manual + CGM) · ⊘ 5a Dexcom Clarity (DEFERRED)
- ✅ 7.1 Trend dashboard · ✅ 7.2 Connections · ✅ 7.3 Personal baseline & drift engine · ✅ 7.3R Baselines & drift redesign + tab move
- ✅ SpO2 dashboard surfacing
- ✅ Oxylink desaturation (ODI + time-below-90 + overnight curve + Overnight Oxygen panel + true event markers + Today min-SpO2 tile)
- ✅ Contour fingerstick parser + CGM-trace fingerstick markers
- ✅ Dashboard 7.1 fixes (Whoop wake-day attribution + Glucose fingerstick fallback + KPI sparkline overflow)
- ✅ Whoop SpO2 rename → `spo2_whoop` + skin_temp add
- ✅ Two-fix patch (TrendChart isolated-point dots + TodayAtAGlance glucose fingerstick fallback)
- ✅ Cardiac BP chart — ACC/AHA per-metric zones + combined category readout
- ✅ Slice 6 — Labs PDF import (LLM extraction + human review + Labs section with trends)
- ✅ Labs large-file fix — direct-to-Storage upload + mixed-document prompt
- ✅ Labs marker-system maturation — auto-canonicals + AI-proposed ranges + computed flags + remembered-range store
- ✅ Labs UI split — `/labs` = import tool only; dashboard 4th tab "Labs" = data viz
- ✅ **Doctor record (`/report`) + editable medications list**
- ✅ **Lab markers — clinical-target tracking** (LAB_TARGETS + evaluateLabMarker + Lp(a) reported fallback + report page-1 + Labs tab summary strip & goal lines)
- ✅ **Medication adherence — daily one-tap** (migration_011 + MedAdherencePanel + /medications heat-strip + /report self-reported line; honest two-number math)
- ✅ **Platform alerts & reminders** (Resend email primitive + daily pipeline-health digest cron + daily adherence-nudge cron + /diagnostics pipeline-health table; resolves the "cron monitoring" Pending item)
- ✅ **Correlations engine revamp — lagged-association discovery + rebuilt Explorer** (detrend + autocorrelation-corrected FDR + curated/optional-LLM confounder layer; VAR/LMM = Phase 2)
- ✅ **Food diary + auto-macros** (migration_012 + Sonnet estimate → review → commit + meals_daily CTE + eaten-day attribution + meal markers on CGM trace + engine adjustments)
- ✅ **Code-review Phase 1 remediation** (PHI-read guards on every 'use server' export + App Router error boundaries + 3 correctness bugs + migration_013 perf indexes + trivial cleanups)
- ✅ **Code-review Phase 2 remediation — test suite + CI + README** (vitest, 134 assertions over the stats/drift/clinical-classifier core + file-drop parsers + gst; GitHub Actions CI with lint advisory; real README)
- ✅ **Lint cleanup follow-on — CI fully green** (all ~50 pre-existing lint errors genuinely fixed, zero disables; LDL near-goal boundary bug fixed; CI lint flipped back to hard-blocking)
- ✅ **Deep audit 2026-07-01 — top 4 fixed** (C1 unauth OAuth routes, H7 open redirect, H4 last_meal_to_sleep_min NULL-vs-0, H5 hidden clinical-low breach; +13 tests; 4 HIGH / 14 MEDIUM / 17 LOW remain — see CLAUDE.md "Known bug backlog")
- ✅ **Deep audit — ingestion-reliability cluster fixed 2026-07-06** (H1 frontier lookback, H2 Whoop cycle-buffer parity, H3 frontier freeze/unbounded window, H6 stale-CGM-as-live + TIR coverage; +12 tests; 0 CRITICAL / 0 HIGH remain, 14 MEDIUM / 17 LOW — see CLAUDE.md "Known bug backlog")
- ✅ **Mobile device-width pass 2026-07-07** (CSS/UI only — overflow-x:clip safety net + min-width:0 root-cause fixes + table scroll wrappers + header/tabnav wrap + /report screen query + /food & /medications mobile grids; zero horizontal scroll at 320px every route, desktop + print unchanged; gotcha #160)
- ✅ **Deep audit — LOW tier cleared 2026-07-07 → -30** (L1–L17: 16 fixed 2026-07-07, gotchas #162–165, +17 tests → 175; L16a resolved 2026-07-30 with the 04:00-GST meal-day boundary, gotcha #166; backlog now 0 CRITICAL / 0 HIGH / 14 MEDIUM / 0 LOW, nothing flagged — see CLAUDE.md. migration_003 re-run again after the L16a change)
- → **M-tier bug backlog triage/fixes** OR **Withings weight extension** (small follow-on) — NEXT (Irfan's call on which)
- ⬜ Anchor population (post-rehab; NB fix backlog M1 first); confirm Dr. Jose floors + ODI severity + skin_temp threshold + lab targets; Slice 8 — Discipline layer; fasting cross-check (Contour vs CGM-derived)

## Data state
Unchanged for existing sources. **New surface area**:
- `lab_panels` + `lab_values` (reserved in migration_001) — populated via `/labs`.
- `lab_marker_aliases` (NEW, migration_008) — learns `raw_marker_name → marker_slug` per (lab, raw_name) on every commit; auto-applies on subsequent uploads.
- `sources('labs')` row seeded.
- **Storage bucket required:** `lab-reports` (private, ~25 MB limit) — created in the Supabase Studio UI, NOT via migration (storage DDL isn't user-writeable in SQL editor).
- `medications` (migration_010) — Irfan's regimen, deactivate-not-delete.
- `med_adherence` (NEW, migration_011) — one row per GST calendar day, tri-state status; populated by the new one-tap panel. Empty until Irfan starts tapping.

## Next action
**Irfan's call:** (a) continue the deep-audit backlog (CLAUDE.md "Known bug backlog" — all CRITICAL/HIGH done; M-tier next, M1 before the first anchor is set), or (b) **Withings weight extension** (small follow-on) — extend `adapters/withings/{api,index}.ts` + `app/api/refill/withings/route.ts` (gotcha #25 — they must agree) to request `meastype=1` and write `metric_type='weight'`, canonical `kg`. No new migration. Unblocks the Weight panel, Weight KPI, BP-vs-weight Correlation preset, and the weight drift signal.

~~**Also pending from this session (Irfan, manual):** re-run `migration_003` in Supabase to pick up the H4 fix.~~ — ✅ DONE 2026-07-01, Irfan confirmed the migration is run. (Not independently re-verified from this session — no live DB access here. Worth a spot-check next time the dashboard's open: `SELECT last_meal_to_sleep_min FROM daily_metrics WHERE date = CURRENT_DATE` should read NULL, not 0, for today.)

## Open items (non-blocking)
- **Deep-audit bug backlog** — 0 CRITICAL / 0 HIGH / 14 MEDIUM / 0 LOW remain in CLAUDE.md "Known bug backlog"; all CRITICAL/HIGH/LOW cleared (2026-07-01 through -30), nothing flagged. M1 (drift anchor/rolling-baseline mix) is the one to fix BEFORE the first anchor is ever set.
- **Food-logging UI "today" grouping still uses midnight GST** (`fetchTodayMeals`/`FoodClient`), while the correlation view now uses the 04:00 meal-day boundary (L16a). Deliberate scope split; unify with a shared `mealDay(ts)` helper if the UI should match. Non-blocking.
- **Withings native `lastupdate` sync param** — deferred alternative to the H1 lookback-widening fix; sync-time-based instead of measurement-time-based, would be the exactly-correct fix for Withings specifically. Not pursued now (extra per-source API-contract complexity, harder to verify without live access) — revisit if the 7-day lookback ever proves insufficient in practice.
- **Long-outage recovery is manual** — the H1/H3 fix caps the frontier lookback (`MAX_LOOKBACK`: 30d Whoop/Withings, 14d Nightscout) so the cron can't wedge, but an outage longer than the cap leaves a gap the cron won't self-heal. Recovery path is the refill route (`POST /api/refill/{source}` with an explicit `fromDate`), not waiting on the cron — gotcha #159.
- Anchor population — `/baselines` set-anchor form built; populate post-rehab (fix backlog M1 — drift anchor/rolling baseline mix — BEFORE setting the first anchor).
- Confirm Dr. Jose low-side floors (BP / SpO2) + ODI severity + skin_temp threshold — provisional.
- Withings weight extension — small follow-on.
- Lab markers as drift metrics (LDL drift etc.) — deferred follow-on, builds on the Labs section.
- Doctor-record export — separate, builds on labs + meds.
- Fasting cross-check (Contour vs CGM-derived) — deferred follow-on.
- Re-mapping UI for lab_marker_aliases — v1 has ON CONFLICT DO NOTHING (first commit wins); a future UI to update aliases would make it possible to fix a mis-mapping after the fact.
- Range-wide meter-vs-sensor view — Glucose panel's CGM trace is fixed 24h.
- xDrip+ ongoing CGM capture — new G7 sensor pending.
- Daily / metric_drift views scan full history per query — future perf item once CGM accrues months.
- ~~No cron failure alerting.~~ — ✅ Daily `/api/cron/health-check` digest emails when any API-pull source is stale. Set `RESEND_API_KEY` + `ALERT_EMAIL_TO` on Vercel to activate. File-drop sources excluded by design (user-action-dependent, staleness is normal there).
- `middleware.ts` → `proxy.ts` rename (Next 16 deprecation warning only).
- Orphan `ingestion_log` rows from timed-out runs — harmless.
- CLAUDE.md cleanup carry-over — "two stale Slice 4 parked lines (~99 + ~251)" — still couldn't locate them.
